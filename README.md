# Rendu — travaux pratiques GNS3

Ce dépôt regroupe les TPs réalisés dans le cadre du module. Chaque TP est dans son propre dossier.

## Contenu

| TP | Dossier | Description rapide |
|----|---------|-------------------|
| **TP1** | [`TP1/`](TP1/) | Routage, NAT, DHCP (captures `part1`–`part4`, configs dans `part4`). |
| **TP2** | [`TP2/`](TP2/) | Attaques DHCP / ARP (scripts Python, captures `part1`–`part3`). |
| **TP3** | [`TP3/`](TP3/) | Sécurisation L2 (DHCP snooping, DAI, IP source guard, STP ; configs `part5`). |
| **TP4** | [`TP4/`](TP4/) | Configurations équipements (`*.txt`) et captures (`stp.pcap`, `ping_hsrp.pcap`). |

---

*Archive Moodle : zip de l’ensemble de ce dépôt (racine = ce fichier `README.md`).*
